<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login 
        .contenedor-formulario
            h1= nombrePagina 
            p Coloca tu nueva contraseña
            form.caja-login(method="POST")
                .campo 
                    label(for="password") Password: 
                    input(type="password" name="password" placeholder="Nuevo password password" value= password required)
                .campo.enviar
                    input.boton(type="submit" value="Restablecer password")
